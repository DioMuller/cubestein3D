#pragma once
#include "Entity.h"
#include "Player.h"

class Shot : Entity
{
	////////////////////////////////////////
	// Constructor / Destructor
	////////////////////////////////////////
	public:
		Shot(Vector position, Vector direction);
		~Shot();

	////////////////////////////////////////
	// Methods
	////////////////////////////////////////
	public:
		void Render(long delta, Renderer* renderer);
};

